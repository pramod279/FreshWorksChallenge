/**
 * This Gradle Contains App Version Related Information
 */
def appInfo = [:]

/**
 * Application ID
 */
appInfo.applicationID = "com.freshworks.challenge"

/**
 * Application Name
 */
appInfo.applicationName = "FreshWorks Challenge"

/**
 * Build Version Information
 */
appInfo.versionCode = 1
appInfo.versionName = "1.0"

ext.appInfo = appInfo

/**
 * Build Version Information
 */
def buildVersion = [:]

/**
 * Minimum & Maximum Supported Versions for the App
 */
buildVersion.minSdk = 21
buildVersion.targetSdk = 31

ext.buildVersion = buildVersion